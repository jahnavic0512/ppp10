name: pipeline

on: push

jobs:
 build:
  runs-on: windows-latest
  steps:
  - uses: actions/chechout@v4
  - uses: actions/setup-python@v5
    with:
       python-version: '3.12'
  - run: python app.py
 
  

